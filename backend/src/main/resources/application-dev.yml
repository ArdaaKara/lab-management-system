spring:
  datasource:
    url: jdbc:mysql://localhost:3306/clms_dev?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: clms
    password: clms
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: dev-secret-key-minimum-32-characters-long-for-hs256-algorithm

logging:
  level:
    com.clms: DEBUG
    org.springframework.security: DEBUG

springdoc:
  swagger-ui:
    enabled: true
